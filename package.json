{
  "name": "ho-so-den-ai-detective",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && mkdir -p dist/.next dist/server dist/.openai && cp -R .next/standalone/. dist/ && cp -R .next/static dist/.next/static && cp -R public dist/public && cp sites-entry.js dist/server/index.js && cp .openai/hosting.json dist/.openai/hosting.json",
    "start": "next start"
  },
  "dependencies": {
    "next": "15.5.9",
    "react": "19.0.4",
    "react-dom": "19.0.4"
  }
}
